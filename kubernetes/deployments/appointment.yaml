apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
    spec:
      containers:
      - name: appointment
        image: 283904064984.dkr.ecr.eu-north-1.amazonaws.com/shivam-hospital/appointment-service:latest
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: 50m
            memory: 100Mi
          limits:
            cpu: 500m
            memory: 512Mi
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: backend
spec:
  selector:
    app: appointment-service
  ports:
  - port: 8000
    targetPort: 8000

