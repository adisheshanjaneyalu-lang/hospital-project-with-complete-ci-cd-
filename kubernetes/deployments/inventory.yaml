apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      containers:
      - name: inventory
        image: 283904064984.dkr.ecr.eu-north-1.amazonaws.com/shivam-hospital/inventory-service:latest
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: backend
spec:
  selector:
    app: inventory-service
  ports:
  - port: 8000
    targetPort: 8006

